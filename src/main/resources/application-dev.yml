spring:
  config:
    import: aws-parameterstore:/chalpu-platform/dev/
    activate:
      on-profile: dev
      
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:mysql://${db-host}:${db-port}/${db-name}?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: ${db-username}
    password: ${db-password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 프로덕션에서는 스키마를 자동으로 변경하지 않도록 validate 사용
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${kakao-client-id} # 웹용 클라이언트 ID
            client-secret: ${kakao-client-secret}
            redirect-uri: "https://platform.chalpu.com/api/login/oauth2/code/kakao"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - profile_nickname
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

jwt:
  secret: ${jwt-secret}
  expiration: 3600000
  refresh-token-expiration: 604800000

server:
  port: 8080
  forward-headers-strategy: native
  tomcat:
    use-relative-redirects: true

#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info,prometheus
#  endpoint:
#    health:
#      show-details: when-authorized
#    prometheus:
#      enabled: true
#  prometheus:
#    metrics:
#      export:
#        enabled: true

# OAuth2 Redirect URLs
oauth2:
  redirect:
    success-url: ${oauth2-redirect-success-url}
    failure-url: ${oauth2-redirect-failure-url}

cloud:
  aws:
    s3:
      bucket: ${s3-bucket}
    cloudfront:
      domain: ${cloudfront-domain} 
    region:
      static: ap-northeast-2
    stack:
      auto: false

# Sentry Configuration
sentry:
  dsn: ${sentry-dsn}
  environment: dev
  traces-sample-rate: 1.0
  enable-tracing: true
  send-default-pii: false
  attach-threads: true
  attach-stacktrace: true

# Gemini API Configuration
gemini:
  api-key: ${gemini-api-key}
  model: gemini-2.0-flash-latest
  api-url: https://generativelanguage.googleapis.com/v1beta
